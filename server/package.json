{
  "name": "nexus-server",
  "version": "1.0.0",
  "description": "Mini servidor proxy para Nexus: IA real con memoria + base de datos local",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
